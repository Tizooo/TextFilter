name: TextFilter
version: '1.0.0'
main: me.tizo.textfilter.TextFilter
api-version: '1.21'
commands:
  textfilter:
    description: Reload the textfilter configuration
    usage: /textfilter reload
    permission: textfilter.reload
    permission-message: You don't have permission to execute this command.